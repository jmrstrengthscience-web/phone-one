Phorce One - Minimal Vite React

This workspace contains a minimal Vite + React setup so the provided `App` component can run locally.

Run locally:

```bash
cd /Users/coachrasberry/Desktop/PhorceOne
npm install
npm run dev
```

Notes:
- The original attachment suggested Create React App. I created a lightweight Vite setup instead for faster startup. If you prefer CRA, tell me and I will switch.
# phone-one
